import React from "react";
import TodoItem from './TodoItem'

const TodoList = () => {
  return(
      <section>
          <h2>todo list</h2>
          <TodoItem/>
      </section>
      )
};

export default TodoList;
